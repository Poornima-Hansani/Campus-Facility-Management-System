PORT=5000

# Option A — MongoDB on your machine (default for development)
MONGO_URI=mongodb://127.0.0.1:27017/unimanage

# Option B — MongoDB Atlas (use the exact URI from Atlas UI; do not use placeholder hostnames)
# MONGO_URI=mongodb+srv://USERNAME:PASSWORD@cluster0.ABCDE.mongodb.net/unimanage?retryWrites=true&w=majority
