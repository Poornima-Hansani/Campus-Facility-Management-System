services:
  mongodb:
    image: mongo:7
    container_name: unimanage-mongo
    ports:
      - "27017:27017"
    volumes:
      - unimanage_mongo_data:/data/db
    restart: unless-stopped

volumes:
  unimanage_mongo_data:
